# local Claude Code settings (per-developer)
.claude/settings.local.json

# eval run artifacts
.eval-run/
evals/last-run.jsonl
evals/.before.json

# story saves + web-session transcript (generated during play)
**/saves/
**/.session/

# your stories (your content; not part of the distribution) — keep only the README
/stories/*
!/stories/README.md

# OS
.DS_Store
